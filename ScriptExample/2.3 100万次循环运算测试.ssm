！循环程序      

使用这些库：“Sevm.System”；

定义一些变量：系统时间，系统时间二，时间差，这个数，输出结果；             

获取系统运行毫秒数，结果给系统时间；

定义 目标数，等于10000；
定义 累加和，等于0；
定义 临时数，等于0；

定义一个叫做循环一的判断，条件是临时数小于等于目标数：
满足条件的话：
    让累加和，加上临时数；
    让临时数，加上0.01；
    然后重新判断循环一；
结束判断定义;

获取系统运行毫秒数，结果给系统时间二；
让时间差，等于系统时间二，减去系统时间；

让输出结果，等于“累加和等于”，连接累加和，连接“ 计算时间为”，连接时间差，连接“毫秒”；
使用控制台换行输出，内容是输出结果；