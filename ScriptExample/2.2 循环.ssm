！循环程序      

定义一些变量：这个数，输出结果；             

先使用控制台输出，"输入一个数："；
再使用控制台读取数字，结果给这个数;  

定义 累加和，等于0；
定义 临时数，等于1；

定义一个叫做循环一的判断，条件是临时数小于等于这个数：
满足条件的话：
    让累加和，加上临时数；
    让临时数，加上1；
    然后重新判断循环一；
结束判断定义;

让输出结果，等于“累加和等于”，连接累加和；
使用控制台换行输出，内容是输出结果；
