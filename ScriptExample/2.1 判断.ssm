！判断程序      

定义 这个数；              

先使用控制台输出，"输入一个数："；
再使用控制台读取数字，结果给这个数;  

定义一个判断，条件是这个数等于100：
满足条件的话：使用控制台换行输出，内容是“这个数等于100”；
不满足的话：
    定义一个判断，条件是这个数大于100：
    满足条件的话：使用控制台换行输出，内容是“这个数大于100”；
    不满足的话：使用控制台换行输出，内容是“这个数小于100”；
    结束判断定义;
结束判断定义;          