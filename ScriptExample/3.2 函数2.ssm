！函数定义

定义一个叫做输出的函数，参数有输出内容：
    使用控制台换行输出，内容是输出内容；
结束函数定义；

定义 这个数；              

先使用控制台输出，"输入一个数："；
再使用控制台读取数字，结果给这个数;  

添加一个判断，条件是这个数大于10：
满足条件的话：输出，输出内容是“这个数大于10”；
不满足的话：输出，输出内容是“这个数小于等于10”；
结束判断；        